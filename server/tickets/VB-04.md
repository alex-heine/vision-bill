| Ticket ID | Type | Description | Dependencies | Goal/Acceptance Criteria |
| :--- | :--- | :--- | :--- | :--- |
| **QC-01** | Utility | Implement Pre-LLM Image Quality Check Service | `StorageService`, OpenCV installed. | Create a function that accepts an image and returns a quality score (e.g., 0-100). The service will fail the upload if sharpness or contrast falls below predefined thresholds, returning actionable feedback to the user ("Too blurry," "Glare detected"). |

