import pytest
from src.vision_bill.provider.db.postgres import PostgresProvider

@pytest.mark.asyncio
async def test_database_connection():
    # Test if the provider can connect to a mock/test DB
    pass
